<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fakten statt Mythen</title>
  
  <style>
    html, body {
      height: 100%;
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
      /* Pastell-Farbverlauf Hintergrund */
      background: linear-gradient(135deg, #8fbcd4, #f5e6c8);
      /* Heller Text, gut sichtbar */
      color: #ffffff;
      text-align: center;
    }

    h1 {
      margin-top: 30px;
      font-size: 2.2em;
      text-shadow: 1px 1px 4px rgba(0,0,0,0.5);
    }

    h2 {
      margin-top: 10px;
      font-size: 1.2em;
      font-weight: normal;
      text-shadow: 1px 1px 3px rgba(0,0,0,0.3);
    }

    p {
      max-width: 700px;
      margin: 15px auto;
      line-height: 1.5;
      text-shadow: 0.5px 0.5px 2px rgba(0,0,0,0.3);
    }

    /* Timeline Galerie horizontal scrollbar */
    .gallery {
      display: flex;
      overflow-x: auto;
      gap: 15px;
      padding: 20px;
      margin: 40px 0;
    }

    .gallery img {
      height: 400px;
      border-radius: 8px;
      box-shadow: 0 3px 10px rgba(0,0,0,0.4);
