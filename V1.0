<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Fakten statt Mythen: Die Geschichte im Zeitstrahl</title>
<style>
body {
  margin: 0;
  font-family: Arial, sans-serif;
  background: linear-gradient(120deg, #a2c1d8, #c3d9e8);
  color: #fff;
  overflow: hidden; /* verhindert das Zoomen der ganzen Seite */
}

header {
  text-align: center;
  padding: 30px;
}

header h1 {
  margin: 0;
  font-size: 2.5em;
}

header p {
  margin-top: 10px;
  font-size: 1.2em;
  color: #f0f0f0;
}

/* Timeline-Wrapper */
#timeline-wrapper {
  width: 100%;
  height: 80vh;
  overflow: hidden;
  position: relative;
  cursor: grab;
}

/* Timeline selbst */
#timeline {
  display: flex;
  height: 100%;
  transform-origin: 0 0; /* Zoom an Mausposition wird später berechnet */
  transition: transform 0.1s ease-out;
}

#timeline img {
  height: 100%;
  margin: 0;
  padding: 0;
  display: block;
  flex-shrink: 0; /* verhindert dass Bilder schrumpfen */
  object-fit: cover;
}

footer {
  padding: 20px;
  text-align: center;
  background: rgba(0,0,0,0.3);
}

footer a, footer button {
  color: #fff;
  text-decoration: none;
  margin: 5px;
}

footer button {
  background-color: #0070ba;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
  border-radius: 5px;
}
</style>
</head>
<body>

<header>
  <h1>Fakten statt Mythen: Die Geschichte im Zeitstrahl</h1>
  <p>Eine Übersicht, die vergleichen lässt und Wahrheit ans Licht bringt</p>
</header>

<div id="timeline-wrapper">
  <div id="timeline">
    <img src="images/Seite1.png" alt="Seite 1">
    <img src="images/Seite2.png" alt="Seite 2">
    <img src="images/Seite3.png" alt="Seite 3">
    <img src="images/Seite4.png" alt="Seite 4">
    <img src="images/Seite5.png" alt="Seite 5">
    <img src="images/Seite6.png" alt="Seite 6">
    <img src="images/Seite7.png" alt="Seite 7">
    <img src="images/Seite8.png" alt="Seite 8">
    <img src="images/Seite9.png" alt="Seite 9">
    <img src="images/Seite10.png" alt="Seite 10">
    <img src="images/Seite11.png" alt="Seite 11">
    <img src="images/Seite12.png" alt="Seite 12">
    <img src="images/Seite13.png" alt="Seite 13">
    <img src="images/Seite14.png" alt="Seite 14">
    <img src="images/Seite15.png" alt="Seite 15">
    <img src="images/Seite16.png" alt="Seite 16">
    <img src="images/Seite17.png" alt="Seite 17">
  </div>
</div>

<footer>
  <p>Hinweis: Bei Fragen und Anregungen gerne an <a href="mailto:bildgesunderworte@gmail.com">bildgesunderworte@gmail.com</a> schreiben.</p>
  <button onclick="window.open('https://www.paypal.com/donate','_blank')">Spenden</button>
</footer>

<script>
const timelineWrapper = document.getElementById('timeline-wrapper');
const timeline = document.getElementById('timeline');

let isDragging = false;
let startX, startY;
let translateX = 0, translateY = 0;
let scale = 1;

// Maus Drag für Verschieben
timelineWrapper.addEventListener('mousedown', (e) => {
  isDragging = true;
  startX = e.clientX - translateX;
  startY = e.clientY - translateY;
  timelineWrapper.style.cursor = 'grabbing';
});

timelineWrapper.addEventListener('mouseup', () => {
  isDragging = false;
  timelineWrapper.style.cursor = 'grab';
});

timelineWrapper.addEventListener('mouseleave', () => {
  isDragging = false;
  timelineWrapper.style.cursor = 'grab';
});

timelineWrapper.addEventListener('mousemove', (e) => {
  if(!isDragging) return;
  translateX = e.clientX - startX;
  translateY = e.clientY - startY;
  updateTransform();
});

// Zoom mit Mausrad
timelineWrapper.addEventListener('wheel', (e) => {
  e.preventDefault();
  const rect = timeline.getBoundingClientRect();
  const offsetX = e.clientX - rect.left; // Mausposition innerhalb der Timeline
  const offsetY = e.clientY - rect.top;

  const zoomFactor = 1 - e.deltaY * 0.0015;
  const newScale = Math.min(Math.max(0.2, scale * zoomFactor), 5);

  // Zoom um Mausposition
  translateX -= (offsetX / scale - offsetX / newScale);
  translateY -= (offsetY / scale - offsetY / newScale);
  scale = newScale;
  updateTransform();
});

function updateTransform() {
  timeline.style.transform = `translate(${translateX}px, ${translateY}px) scale(${scale})`;
}

// Initial Cursor
timelineWrapper.style.cursor = 'grab';
</script>

</body>
</html>
